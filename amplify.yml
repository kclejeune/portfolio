version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install stable
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # IMPORTANT - Please verify your build output directory
    baseDirectory: build/
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
